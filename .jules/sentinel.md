## $(date +%Y-%m-%d) — Enforce bounds on untrusted network stream reads
**Learning:** Parsing untrusted network streams requires validating buffer sizes before locating delimiters to prevent Denial of Service via unbounded memory allocation. Also, when CI secret scanning blocks mock API keys in docs, it is better to sanitize the mock keys (e.g. `<your_api_key_here>`) rather than weakening the secret scan rules.
**Action:** Enforced 64KB header bounds, 1MB data/payload bounds, and ensured correct exception handling on truncated reads in `wakeword.py` and `wakeword_bench.py`. Sanitized dummy API keys in documentation and scripts.
