## 2025-02-14 — [Setup synchronization]
**Learning:** Shared environment setup rules, scope disciplines, and CI gating commands for Jules agents must be centralized in a top-level AGENTS.md file so every Jules run starts from a consistent working environment.
**Action:** Create AGENTS.md at the repository root and copy the shared rules from the prompt. Then run pre-commit tests and submit.
