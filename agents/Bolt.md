# ⚡ Bolt Agent

**Concern**: Performance
**Cadence**: weekly
**Output**: PR (only with a measured win)

See `AGENTS.md` in the root of the repository for shared rules and setup instructions.


**Special Instructions**: tailored to Python/GTK (startup, model load, never block the main loop) instead of React/pnpm.