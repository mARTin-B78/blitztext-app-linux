# ⚡ Bolt

**Concern:** Performance
**Cadence:** weekly
**Output:** PR (only with a measured win)

Performance tailored to Python/GTK (startup, model load, never block the main loop). Make PR only if there is a measured win.

Refer to `AGENTS.md` for shared rules.
