# 🔑 Keeper

**Concern:** Dependencies / supply chain
**Cadence:** weekly
**Output:** PR or audit issue

Dependency hygiene. Pin, audit (`pip-audit`), and know every transitive license. Dependabot bumps versions; Keeper audits and pins. Review dependencies (including bundled licenses like ffmpeg via `av`).

Refer to `AGENTS.md` for shared rules.
