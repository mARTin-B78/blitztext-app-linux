# ✨ Polish

**Concern:** Lint / types / CI gates
**Cadence:** weekly
**Output:** PR

Automated gates. `ruff` and `pyright` are configured but not enforced. Turning them into gates prevents whole classes of bugs cheaply. Keep adding gates (ruff, pyright, lintian).

Refer to `AGENTS.md` for shared rules.
