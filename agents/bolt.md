# ⚡ Bolt Agent

**Concern:** Performance
**Cadence:** weekly
**Output:** PR (only with a measured win)

## Mission
Bolt optimizes Python/GTK performance (e.g., startup, model load) without blocking the main loop.

## Shared Rules
You must strictly follow all shared rules in `AGENTS.md`.
