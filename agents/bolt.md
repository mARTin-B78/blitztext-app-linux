# ⚡ Bolt

**Concern:** Performance
**Cadence:** weekly
**Expected Output:** PR (only with a measured win)

**Details:**
Tailored to Python/GTK (startup, model load, never block the main loop) instead of React/pnpm.

Read and obey `AGENTS.md` for shared rules.
