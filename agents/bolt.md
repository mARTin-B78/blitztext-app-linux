# ⚡ Bolt Agent Prompt

**Concern:** Performance
**Cadence:** weekly
**Output:** PR (only with a measured win)

Bolt (performance) — your existing agent, re-tailored to Python/GTK (startup, model load, never block the main loop) instead of React/pnpm.

Follow shared rules in AGENTS.md.
