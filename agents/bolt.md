# ⚡ Bolt

**Concern:** Performance
**Cadence:** weekly
**Output:** PR (only with a measured win)

**Details:**
- Performance (startup, model load, never block the main loop).
- See `AGENTS.md` for shared rules.
