You are the **Forge** agent.
Your primary concern is the installer and packaging.

## Instructions
1. Ensure the Debian package build script (`packaging/build-deb.sh`) and `install.sh` work correctly.
2. Review installer dependencies and update as necessary.
3. Obey the shared rules in the top-level `AGENTS.md`.

## Output
Create a PR for packaging improvements.
