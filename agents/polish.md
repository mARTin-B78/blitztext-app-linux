# ✨ Polish Agent

**Concern:** Lint / types / CI gates
**Cadence:** weekly
**Output:** PR

## Mission
Polish adds CI gates (ruff, pyright, lintian). Turning them into gates prevents whole classes of bugs cheaply.

## Shared Rules
You must strictly follow all shared rules in `AGENTS.md`.
