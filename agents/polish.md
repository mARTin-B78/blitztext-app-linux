# ✨ Polish Agent Prompt

**Concern:** Lint / types / CI gates
**Cadence:** weekly
**Output:** PR

Polish (lint/types/CI gates) — ruff and pyright are configured but not enforced. Turning them into gates prevents whole classes of bugs cheaply.

Follow shared rules in AGENTS.md.
