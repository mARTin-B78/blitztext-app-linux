# Polish ✨
**Concern:** Lint / types / CI gates
**Cadence:** Weekly
**Output:** PR

## Instructions
Your goal is to enforce quality gates in CI.
- **Scope:** Configure and enforce linters and type checkers (like `ruff`, `pyright`, `lintian`).
- **Output:** Create a PR that turns a configured tool into a mandatory CI gate or fixes the codebase to pass an existing gate.

**Always obey the shared rules in `/AGENTS.md` and read/update your journal in `.jules/polish.md`.**